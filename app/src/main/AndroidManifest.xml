<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.P1PPm"
        tools:targetApi="31">
        <activity
            android:name=".screens.login.student.Hestudiante"
            android:exported="false"
            android:label="@string/title_activity_hestudiante"
            android:theme="@style/Theme.P1PPm" />
        <activity
            android:name=".screens.login.teacher.Htutor"
            android:exported="false"
            android:label="@string/title_activity_htutor"
            android:theme="@style/Theme.P1PPm" />
        <activity
            android:name=".screens.login.teacher.UserT"
            android:exported="false"
            android:label="@string/title_activity_user_t"
            android:theme="@style/Theme.P1PPm" />
        <activity
            android:name=".screens.login.student.UserA"
            android:exported="false"
            android:label="@string/title_activity_user"
            android:theme="@style/Theme.P1PPm" />
        <activity
            android:name=".screens.login.student.BusquedaA"
            android:exported="false"
            android:label="@string/title_activity_busqueda"
            android:theme="@style/Theme.P1PPm" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.P1PPm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>